import './bootstrap';

import * as bootstrap from 'bootstrap';
import 'bootstrap/dist/css/bootstrap.min.css';
window.bootstrap = bootstrap; // Opsional: expose Bootstrap globally if needed for some plugins